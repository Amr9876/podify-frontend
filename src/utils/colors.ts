const PRIMARY = '#000000';
const SECONDARY = '#204099';
const CONTRAST = '#FFFFFF';
const INACTIVE_CONTRAST = 'rgba(255, 255, 255, 0.5)';
const ERROR = '#FF5B14';
const SUCCESS = '#3BEF1E';
const OVERLAY = 'rgba(0, 0, 0, 0.5)';

const colors = {
  PRIMARY,
  SECONDARY,
  CONTRAST,
  INACTIVE_CONTRAST,
  ERROR,
  SUCCESS,
  OVERLAY,
};

export default colors;
